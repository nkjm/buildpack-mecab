#!/usr/bin/env bash
echo "-----> Installing mecab"
mkdir $1/.heroku/src
cd $1/.heroku/src
wget https://github.com/nkjm/buildpack-mecab
tar -C $1/.heroku -xvf buildpack-mecab.tar.gz
rm -rf $1/.heroku/src
